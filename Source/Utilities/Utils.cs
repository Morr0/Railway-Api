namespace rail.util {
    public class Utils {
        /// <summary>
        /// Makes a track of equal lengths outside a station/yard.
        /// </summary>
        /// <returns></returns>
        public static TrackSection[] MakeTrack(int trackLength, int noTracks, string startingName){
            TrackSection[] sections = new TrackSection[noTracks];

            
            TrackSection tempObjBefore = null, tempObj = null;


            for (int i = 0; i < noTracks; i++){

                temp = new TrackSection() 
            }

            return sections;
        }
    }
}